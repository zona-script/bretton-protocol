{
  "name": "bretton-protocol",
  "version": "1.0.0",
  "description": "The Bretton Protocol",
  "scripts": {
    "clean": "rm -rf build",
    "build": "truffle compile",
    "clean-build": "npm run clean && npm run build",
    "test": "npm run clean-build && mocha --exit --recursive tests --timeout 10000"
  },
  "author": "Bretton Protocol",
  "license": "MIT",
  "devDependencies": {
    "@openzeppelin/cli": "^2.8.2",
    "@openzeppelin/contract-loader": "^0.6.1",
    "@openzeppelin/test-environment": "^0.1.4",
    "@openzeppelin/test-helpers": "^0.5.6",
    "@truffle/hdwallet-provider": "^1.0.40",
    "truffle-plugin-verify": "^0.3.11",
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "mocha": "^8.0.1",
    "web3": "^1.2.11",
    "dotenv-flow": "^3.2.0"
  }
}
