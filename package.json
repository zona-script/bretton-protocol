{
  "name": "bretton-protocol",
  "version": "1.0.0",
  "description": "The Bretton Protocol",
  "scripts": {
    "clear-build": "rm -rf build",
    "build": "npm run clear-build && oz compile",
    "test": "npm run build && mocha --exit --recursive test"
  },
  "author": "Bretton Protocol",
  "license": "MIT",
  "devDependencies": {
    "@openzeppelin/cli": "^2.8.2",
    "@openzeppelin/test-environment": "^0.1.4",
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "mocha": "^8.0.1"
  }
}
